class Solution {
    public int solution(String s) {
        int answer = 0;
        // if (s.charAt[0] == '+') {
        //     return Integer.valueOf(s.substring(1));
        // } else if (s.charAt[0] == '-') {
        //     return answer - Integer.valueOf(s.substring(1));
        // }
        
        return Integer.valueOf(s.substring(0));
    }
}